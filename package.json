{
  "name": "secure-asset-portal",
  "version": "1.0.0",
  "description": "A secure web portal for financial asset management",
  "main": "index.js",
  "scripts": {
    "setup": "node setup.js",
    "setup:complete": "node setup.js",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:frontend\"",
    "dev:brave": "concurrently \"npm run dev:backend\" \"npm run dev:frontend:brave\"",
    "dev:backend": "cd backend && npm run dev",
    "dev:frontend": "cd frontend && npm start",
    "dev:frontend:brave": "cd frontend && BROWSER=\"Brave Browser\" npm start",
    "start": "npm run build && NODE_ENV=production npm run start:backend",
    "start:backend": "cd backend && npm start",
    "start:dev": "npm run dev",
    "build": "npm run build:backend && npm run build:frontend",
    "build:backend": "cd backend && npm run build",
    "build:frontend": "cd frontend && npm run build",
    "start:prod": "npm run build && echo '\n🚀 Starting production server...' && echo '\n📱 Opening browser at http://localhost:3000\n' && (sleep 3 && open http://localhost:3000) & NODE_ENV=production npm run start:backend",
    "test": "npm run test:backend && npm run test:frontend",
    "test:backend": "cd backend && npm test",
    "test:frontend": "cd frontend && npm test",
    "install:all": "npm install --no-audit && npm install --prefix backend --no-audit && npm install --prefix frontend --no-audit",
    "db:setup": "psql -f database/schema.sql secure_asset_portal",
    "db:reset": "dropdb --if-exists secure_asset_portal && createdb secure_asset_portal && npm run db:setup",
    "docker:up": "docker-compose up -d",
    "docker:down": "docker-compose down",
    "docker:build": "docker-compose build",
    "docker:logs": "docker-compose logs -f",
    "lint": "eslint backend frontend --ext .js,.jsx,.ts,.tsx",
    "format": "prettier --write \"**/*.{js,jsx,ts,tsx,json,css,md}\"",
    "security:audit": "npm audit && cd backend && npm audit && cd ../frontend && npm audit",
    "admin:create": "node scripts/create-admin.js",
    "open:brave": "./scripts/open-brave.sh"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/username/secure-asset-portal.git"
  },
  "keywords": [
    "asset-management",
    "financial",
    "security",
    "portfolio",
    "react",
    "nodejs",
    "postgresql"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.0",
    "eslint": "^9.0.0",
    "eslint-config-prettier": "^9.0.0",
    "eslint-plugin-react": "^7.35.0",
    "prettier": "^3.0.0"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=8.0.0"
  },
  "overrides": {
    "color-convert": "2.0.1",
    "color-name": "1.1.4",
    "debug": "4.3.4",
    "error-ex": "1.3.2",
    "is-arrayish": "0.3.2"
  },
  "private": true,
  "dependencies": {
    "bcrypt": "^6.0.0",
    "dotenv": "^17.2.2",
    "pg": "^8.16.3",
    "uuid": "^12.0.0"
  }
}
