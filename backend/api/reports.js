const express = require('express');
const router = express.Router();

// Placeholder for reporting endpoints
router.get('/', (req, res) => {
  res.json({ message: 'Reporting endpoints coming soon' });
});

module.exports = router;
